# Sentiment-Analysis
This a Sentiment Analysis Program. It takes in a input of a review and classifies it as negative or positive. It uses a tokenizer for tokenizing the words And a deep neural network for prediction.
